- name: Remove unwanted packages
  ansible.builtin.package:
    name: "{{ bootstrap_host_packages.absent | default([]) }}"
    state: absent

- name: Install desired packages
  ansible.builtin.package:
    name: "{{ bootstrap_host_packages.present | default([]) }}"
    state: present
